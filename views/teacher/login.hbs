<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="stylesheets/index.css">
    <link rel="stylesheet" href="stylesheets/login.css">
    <link rel="stylesheet" href="stylesheets/nav.css">
    <script type="text/javascript" src="javascripts/teacher/login.js" defer></script>
    <title>{{title}}</title>
</head>

<body>
    <header>
        <h1>Welcome to EFPAC's interactive platform</h1>
    </header>
    {{#if signUpSuccess}}
    <h1 class="success">Sign Up Successful! Your account must now be validated by another teacher</h1>
    {{/if}}
    {{#if errors}}
    <section>
        <ul>
            {{#each errors}}
            <li>
                <h1 class="error">{{this.msg}}</h1>
            </li>
            {{/each}}
        </ul>
    </section>
    {{/if}}
    <div id="contenedor">
        <div class="wrap-login">
            <img src="img/logoupm.png" class="center">
            <h1 class="title">Teacher Login</h1>
            <section class="container">
                <div class="lgns">
                    <form id="login-form" class="teacher-form" action="/teacher-login" method="POST" autocomplete="on">
                        <input type="email" name="email" id="email" placeholder="user@upm.es" autocomplete="email"
                            class="emailInput wrap-input input100" required />
                        <input type="password" name="password" id="password" placeholder="Password"
                            autocomplete="current-password" class="wrap-input input100" required />
                        <div>
                            <input type="checkbox" onclick="showPassword()">Show Password
                        </div>
                        <button type="submit" class="btn"> Submit </button>
                    </form>
                </div>
            </section>
            <h4>Don't have an account yet? <a href="teacher-sign-up">Sign Up!</a></h4>{{!-- TODO: delete before roll-out once enough teachers are in--}}
            <h4>Not a teacher? <a href="/">Go Back</a></h4>
        </div>
    </div>
</body>
<script> </script> {{!-- Avoids initial transition bug  --}}

</html>